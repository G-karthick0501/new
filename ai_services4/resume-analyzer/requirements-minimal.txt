# ============================================
# Resume Analyzer Service - Minimal Requirements
# Based on actual usage analysis
# Port: 8000
# ============================================

# Core Web Framework (Working Versions)
fastapi==0.120.0
uvicorn[standard]==0.38.0
python-multipart==0.0.6

# PDF Processing (Actually Used)
PyPDF2==3.0.1
pdfplumber==0.10.0
# fitz (PyMuPDF) - not in pip list but used in utils

# NLP & ML (Actually Used)
nltk==3.9.2
spacy==3.7.2
sentence-transformers==2.2.2
numpy>=1.24.0
scikit-learn>=1.3.0

# PDF Generation (Actually Used)
reportlab==4.4.4
# weasyprint - not in pip list but used in services

# Utilities (Actually Used)
requests==2.31.0
python-dotenv==1.0.0

# Spacy model (install separately)
# python -m spacy download en_core_web_sm
